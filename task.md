Давай сделаем мобильное приложение `/sminex` более премиальным. Начнем с навигации в `src/components/sminex/BottomNav.tsx` и `PhoneFrame.tsx`.

1. Измени `BottomNav`. Отвяжи его от жесткого низа. Сделай его плавающим "доком" (pill-shape): `absolute bottom-6 left-4 right-4 rounded-3xl bg-white/80 backdrop-blur-2xl shadow-[0_8px_32px_rgba(0,0,0,0.1)] border border-white/50`.
2. Убери старый индикатор (ползунок). Вместо него активная иконка должна получать круглую/овальную подложку цвета `bg-[#1D252D]` и становиться белой (`text-white`), а неактивные остаются серыми. Скрой текстовые лейблы, оставь только иконки (для минимализма), либо показывай лейбл только у активной табы.
3. В `PhoneFrame.tsx` убедись, что контент (children) занимает всю высоту и скроллится ПОД навигационным баром (добавь нижний padding контенту, чтобы последняя карточка не перекрывалась доком).
Теперь переработаем `src/components/sminex/DashboardScreen.tsx` для резидента (`ResidentDashboard`), чтобы он выглядел как премиальный консьерж-сервис.

1. Замени верхний блок (приветствие) на компонент "VIP Card". Это должен быть блок с соотношением сторон банковской карты, залитый темным премиальным градиентом (от `#1D252D` к черному) с легкой золотой обводкой (`border-[#8B7355]/30`). Внутри: имя, погода, и плашка с номером квартиры. Текст белый.
2. Переработай "Main service grid" (Домофон, Камеры и т.д.). Вместо обычных иконок сделай их в стиле iOS: крупные скругленные квадраты (`rounded-[1.25rem]`), внутри которых иконка крупнее, с мягким цветным фоном (например, для домофона - синеватый градиент, для услуг - фиолетовый). Добавь им легкую тень и белую внутреннюю границу (inner shadow), имитирующую стекло.
3. Сделай фон всего приложения чуть более глубоким: в `PhoneFrame.tsx` и экранах используй `bg-[#F9F9F8]` (очень светлый теплый серый/песочный), чтобы белые карточки на нем лучше выделялись.
Редизайн экрана `src/components/sminex/SmartHomeScreen.tsx` в стиле Apple HomeKit / топовых PropTech приложений.

1. Карточки устройств (`filteredDevices`): если устройство выключено, оно должно быть матовым (`bg-white/60`). Если включено — карточка должна загораться (например, `bg-[#1D252D]` с белым текстом, или светящийся белый `bg-white` с золотой тенью). Переключатель внутри карточки должен выглядеть как стильный тумблер (iOS style toggle).
2. Слайдер климата: переделай стандартный `<input type="range">` на что-то похожее на Control Center. Толстый трек (`h-12 rounded-2xl`), где залитая часть цвета `bg-amber-500`, а остальная `bg-gray-200`. Значение температуры должно отображаться крупным шрифтом прямо поверх/рядом со слайдером.
3. Сценарии: сделай их более графичными. Добавь им мягкий `radial-gradient`, когда они активны.
Отполируем `IntercomScreen.tsx` и `ServicesScreen.tsx`.

1. В `IntercomScreen`: заглушка видео (`Video feed mock`) должна выглядеть круто. Сделай её на всю ширину (без отступов по бокам или с минимальными), добавь поверх нее красивый градиентный оверлей снизу вверх. Кнопка "Открыть дверь" должна быть огромной, круглой, и при нажатии показывать анимацию "расходящихся волн" (ripple effect с помощью `animate-ping` или кольцевых теней).
2. В `ServicesScreen` карточки консьерж-услуг: добавь картинки или сделай иконки огромными и полупрозрачными на фоне (watermark effect) внутри карточки. Это добавит глубины. Сама цена должна быть выделена золотым цветом (`text-[#8B7355]`).